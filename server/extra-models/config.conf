# config.conf — one line per side-by-side mlx_lm.server.
# Format:  <short-name>  <port>  <hf-repo-id>  [max-tokens]
#
# Each row tells mlx-server.sh to:
#   - start an `mlx_lm.server` process on <port> serving <hf-repo-id>
#     from the local HuggingFace cache (run `hf download <repo>` first)
#   - cap generation at [max-tokens] (4th column, optional). When absent
#     the row inherits $MAX_TOKENS (default 32768). mlx_lm.server's own
#     default is 512, which truncates long answers — hence the floor.
#   - log to    extra-models/logs/<short-name>.log
#   - pidfile   extra-models/pids/<short-name>.pid
#   - expose via models.json as a sibling provider — operator wires
#     `local-mlx-<short-name>` in models.json themselves (the launcher
#     does not edit models.json; see server/README.md for the snippet).
#
# Comments (lines starting with #) and blank lines are ignored.
#
# Examples (uncomment when the model is downloaded into the HF cache):
# codestral    18100  mlx-community/Codestral-22B-v0.1-4bit
# deepseek-v2  18101  mlx-community/DeepSeek-Coder-V2-Lite-Instruct-4bit-mlx
# codellama    18102  mlx-community/CodeLlama-34b-Instruct-hf-4bit-mlx

# Candidate (NOT in the bare-up set): Qwen3.6-35B-A3B 8-bit — session/adversary
# track candidate, behind the 27B as adversary and session-untested, so it is
# kept OUT of the standing RPI set. Uncomment to put it back; while commented,
# neither bare `up` nor `up q36` will start it.
# Heavy thinking model: it spends budget on <think> before the answer, so it
# needs a generous ceiling or long replies get cut off (and a too-small cap
# can return an empty body). Inherits the 32768 default below; add a 4th
# column to override per-row. To suppress thinking entirely, send
# chat_template_kwargs.enable_thinking=false (pi's --thinking flag does NOT
# reach this backend; see provider compat).
# q36          18110  mlx-community/Qwen3.6-35B-A3B-8bit

# Active: Qwen2.5-Coder-32B-Instruct 8-bit — the RPI Code Worker / Implementor
# backend (dense coder, ~35GB, non-thinking). Served on :18111; the
# `local-mlx-coder32b` provider in models.json points here, and the
# coder-run.sh worker pins this exact model id. Bring up on its own with
# `mlx-server.sh up coder32b` (bare `up` also starts it). The row short-name is
# model-agnostic (`coder32b`) so the slot can host a different dense coder later.
coder32b 18111 mlx-community/Qwen2.5-Coder-32B-Instruct-8bit

# DEFAULT coder + adversary backend (128GB): Gemma-4-31B-it QAT 4-bit on its own
# port :18112 (the `local-mlx-gemma4` provider points here). Separate port,
# never shares the :18111 coder slot, so pi's provider->port->model mapping is
# unambiguous. ~22GB resident (QAT 4-bit; lighter and faster than the retired
# 8-bit, no eval-v2 regression as adversary); like the 32B it is one-track with
# the 80B. Thinking is OFF by default, toggled per-run via pi --thinking (the
# provider's qwen-chat-template compat sends chat_template_kwargs.enable_thinking;
# default off avoids the reasoning-stream pi heap OOM, and benchmarks strong as
# adversary). For the container-harness guest to reach it, launch the host server
# bound to 0.0.0.0:  `HOST=0.0.0.0 mlx-server.sh up gemma4`.
#
# NO MTP draft by default. The gemma MTP speculative-decode head (bf16
# gemma4_assistant) targeted the now-removed 8-bit weights and is retired as a
# serving default; this row launches with no draft. The generic
# MLX_MTP_DRAFT_<NAME> infrastructure remains model-agnostic — see ENVIRONMENT.md
# §9 for the decoupled/dormant drafting state.
gemma4 18112 mlx-community/gemma-4-31B-it-qat-OptiQ-4bit
